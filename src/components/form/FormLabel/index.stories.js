import React from "react";
import FormLabel from "./index";

export default { title: "Components|Form|Form Label", component: FormLabel };

export const FormLabelDefault = () => {
    return <FormLabel title="Name" />;
};
